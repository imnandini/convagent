[project]
name = "convagent"
version = "0.1.0"
description = "this is sample conversational gen ai project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain>=1.3.4",
    "langchain-text-splitters>=1.1.2",
    "pytest>=9.0.3",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
