[project]
name = "convagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain>=1.3.4",
    "langchain-text-splitters>=1.1.2",
    "python-dotenv>=1.2.2",
]
